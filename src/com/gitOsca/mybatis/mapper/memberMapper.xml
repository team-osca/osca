<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<select id="selectEmail" resultType="string">
		select member_email from tbl_member where member_email = #{memberEmail}
	</select>
	
<insert id="insert">
		insert into tbl_member
		(member_name, member_email, member_password, member_phonenumber, member_role)
		values( #{memberName}, #{memberEmail}, #{memberPassword}, #{memberPhonenumber}, #{memberRole})
	</insert>

</mapper>